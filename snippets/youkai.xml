<?xml version="1.0" encoding="UTF-8"?>
<manifest>

    <remote name="yk"
        fetch="https://github.com/youkai-b"
        revision="13-ld" />

    <!-- Bootable -->
    <project path="bootable/recovery" name="los_bootable_recovery" remote="yk" />

    <!-- Build -->
    <project path="build/make" name="los_build" remote="yk">
        <copyfile src="core/root.mk" dest="Makefile" />
        <linkfile src="CleanSpec.mk" dest="build/CleanSpec.mk" />
        <linkfile src="buildspec.mk.default" dest="build/buildspec.mk.default" />
        <linkfile src="core" dest="build/core" />
        <linkfile src="envsetup.sh" dest="build/envsetup.sh" />
        <linkfile src="target" dest="build/target" />
        <linkfile src="tools" dest="build/tools" />
    </project>

    <!-- Frameworks -->
    <project path="frameworks/base" name="los_fwb" remote="yk" />

    <!-- Manifest -->
    <project path="manifest" name="los_android" remote="yk" />

    <!-- Packages -->
    <project path="packages/apps/Settings" name="los_settings" remote="yk" />

    <!-- System -->
    <project path="system/core" name="los_system_core" remote="yk" />
</manifest>
